source: site/internals/static/img/evidence/062d0e1193775fb9eeadb575a5b30dd07b312705a1d97f4c216f9c665202f80c.jpg
status: done
engine: gemini-flash-latest
provider: gemini
generated: 2026-08-20T12:41:33.440Z
kind: image
description: >-
  ## Overview

  This is a vertical mobile screenshot taken on an iPhone showing a Google
  Trends webpage in the Safari web browser. The search query displayed is
  "Hunter kozak" set to the region "Israel" over the "Past 90 days." The
  "Interest over time" line chart shows a flat baseline of near-zero interest
  with a single sharp, isolated vertical spike reaching the maximum value of 100
  in late July.


  ## Hyper-Detail

  At the very top of the screen is the iOS status bar:

  - On the left: digital clock displaying `5:21` accompanied by a location
  services arrow icon.

  - On the right: cellular signal bars (4 bars), Wi-Fi icon, and a battery icon
  indicating `85` percent charge.


  Below the status bar is the Safari address bar interface:

  - A square tabs/sidebar icon on the far left.

  - A central URL bar displaying `trends.google.com`.

  - A reload icon (circular arrow) on the far right.


  Below the address bar is the Google Trends mobile header:

  - A hamburger menu icon (three horizontal lines) on the left, followed by the
  text `Explore`.

  - On the right side of this header row: a share node icon and a speech bubble
  with an exclamation mark (feedback) icon.


  Below the header row:

  - A white card container showing a blue dot followed by the text `Hunter
  kozak` on the first line, and `Search term` in gray text below it. On the
  right edge is a vertical three-dot menu icon.

  - Below this card is a light blue rounded rectangular button spanning the
  width containing a gray `+` icon and the uppercase text `COMPARE`.

  - Below the compare button is a filter indicator: a horizontal filter sliders
  icon followed by the text `Israel, Past 90 days`.


  The main visible content section is an "Interest over time" card:

  - White rounded rectangular card background.

  - Header text `Interest over time` on the left, and a vertical three-dot menu
  icon on the right.

  - A 2D line graph with light gray horizontal grid lines and vertical y-axis
  labels on the left: `100`, `75`, `50`, `25`.

  - Along the horizontal x-axis at the bottom are date labels in gray: `Jul 11`,
  `Aug 8`, `Sep 5`, `Oct 3`.

  - The plotted line is bright blue (#1A73E8). It sits flat at 0 along the
  bottom for the entire duration, except for a single sharp peak rising straight
  up to `100` between July 11 and August 8 (around late July), and immediately
  dropping back down to 0.


  Below the "Interest over time" card:

  - The top edge of the next section card is visible: text `Interest by
  subregion`, a dropdown selector button containing `Subregion` with a downward
  arrow, and a vertical three-dot menu.


  Bottom of the screen shows the Safari navigation toolbar:

  - Back arrow `<` (active/blue), Forward arrow `>` (inactive/gray), Share
  square-with-arrow icon (blue), Open book bookmarks icon (blue), and Tab
  switcher overlapping squares icon (blue).

  - At the very bottom is the black iOS home bar indicator line.


  ## Composition

  - The entire image is a 2D graphical digital screenshot filling 100% of the
  frame.

  - **Top 15% (FRAME-TOP):** iOS status bar, Safari address bar, and Google
  Trends navigation header.

  - **Upper-Midground (~15% to 35%):** Search term indicator card, "+ COMPARE"
  button, and location/date filters.

  - **Center-Midground (~35% to 80%):** "Interest over time" graph card,
  occupying roughly 45% of the frame.

  - **Lower-Midground (~80% to 90%):** Partial view of "Interest by subregion"
  card.

  - **Bottom 10% (FRAME-BOTTOM):** Safari navigation toolbar and home bar.

  - Visual weight is centered around the bright blue spike on the graph located
  slightly to the left of center in the graph card.


  ## Location

  - (indeterminate) / Digital interface.

  - Search parameters reference the country Israel.


  ## Setting & Place

  - Digital user interface / Mobile web browser (Safari on iOS).


  ## Lighting

  - Flat, digital rendering. Screen display interface with no physical lighting,
  shadows, or practical sources. High-contrast white cards against a very light
  gray/blue-tinted background.


  ## Cinematography

  - Direct digital screen capture / UI screenshot. No camera, lens, or optical
  perspective involved.


  ## People

  No people in frame.


  ## Objects & Text

  **Legible Text:**

  - `5:21`

  - `85`

  - `trends.google.com`

  - `Explore`

  - `Hunter kozak`

  - `Search term`

  - `+ COMPARE`

  - `Israel, Past 90 days`

  - `Interest over time`

  - `100`, `75`, `50`, `25`

  - `Jul 11`, `Aug 8`, `Sep 5`, `Oct 3`

  - `Interest by subregion`

  - `Subregion`


  **Identifiable Brands/Objects:**

  - Apple iOS UI elements

  - Apple Safari browser UI

  - Google Trends UI


  ## Colors & Mood

  - **Palette:** Predominantly clean white (`#FFFFFF`), light grayish-blue
  background (`#F1F3F4` to `#F8F9FA`), dark charcoal text (`#202124`), and
  Google standard accent blue (`#1A73E8`).

  - **Mood:** Analytical, neutral, minimalist, informational.
