source: site/internals/static/img/evidence/28532457f261668db9f0588ff5a763796ced31b4e64c2ed6ace1eed4669c4660.jpg
status: done
engine: gemini-flash-latest
provider: gemini
generated: 2026-08-20T12:44:01.034Z
kind: image
description: >-
  ## Overview

  This image is a digital graphic/document displaying a comprehensive tabular
  flight log or aircraft tracking history for an aircraft identified as
  "SU-BTT". The table is split into two major multi-column sections containing
  dates ranging from late 2024 through late 2025, origin and destination
  airports, flight numbers or identifiers, flight durations, and
  departure/arrival timestamps. The visual design is clean and minimalist,
  featuring a pure white background with dark grey text, blue highlighted
  airport codes, and subtle horizontal divider lines.


  ## Hyper-Detail

  The document is laid out in a dual-column structured list format:

  - **Left Column Data**: Contains 23 flight log entries dating backwards from
  27 May 2025 to 26 Nov 2024.
    - Columns represent: Date & Origin, Destination, Flight/Callsign Code, Flight Duration, Scheduled Departure/Arrival, and Actual Departure/Arrival times.
    - Prominent destinations and origins on the left include Cairo (`CAI`), Medina (`MED`), Jeddah (`JED`), El Arish (`AAC`), Hurghada (`HRG`), and Muscat (`MCT`).
    - Callsigns visible include `(SUBTT)` and `EGY239`, with several rows showing an en-dash `—` for missing or non-applicable callsign data.
  - **Right Column Data**: Contains 32 flight log entries dating from 18 Nov
  2025 down to 14 Jun 2025.
    - This section shows a heavy concentration of international and regional flights, including destinations such as Paris (`LBG`), Minot (`MOT`), Provo (`PVU`), Wilmington (`ILG`), Lincoln (`LNK`), Omaha (`OMA`), and Sharm el-Sheikh (`SSH`), as well as regular routes involving Cairo (`CAI`), Medina (`MED`), and Jeddah (`JED`).
  - **Watermarks / Identifiers**:
    - In the lower-left empty quadrant below the left data table, there is a large, bold, sans-serif black text label: `SU-BTT`.
    - In the middle of the right table (between columns representing flights in Sep/Oct 2025), a vertically oriented black text reading `SU-BTT` is placed sideways.
  - **Formatting Elements**:
    - Horizontal light-grey rules (`#E5E7EB` or similar faint tone) divide each flight entry row.
    - Airport names are rendered in dark grey/black text accompanied by 3-letter IATA airport codes enclosed in light blue/cyan font within parentheses, e.g., `Cairo (CAI)`.
    - Durations and timestamps are written in standard 12-hour/24-hour style notation (e.g., `8:11 PM`, `7:51 PM`, `1:35`, `0:20`).

  ## Composition

  - **Layout Grid**: Two vertical blocks of data tables spanning side-by-side. 
    - The left table occupies approximately 45% of the frame width (FRAME-LEFT to FRAME-CENTER).
    - The right table occupies approximately 50% of the frame width (FRAME-CENTER to FRAME-RIGHT).
    - A vertical white margin separates the two tables.
  - **Visual Weight**: Balanced evenly across the page through strict grid
  alignment of text data. The bold text `SU-BTT` anchors the bottom left (~8%
  frame area) and mid-right (~4% frame area, rotated 90 degrees).

  - **Depth of Field**: Flat, two-dimensional document; 100% of the image is in
  sharp, digital focus.


  ## Location

  (indeterminate). The document logs global aircraft movement with frequent
  stops in Cairo, Egypt (`CAI`), Saudi Arabia (`MED`, `JED`), France (`LBG`),
  and various regional airports in the United States (`ILG`, `LNK`, `OMA`,
  `PVU`, `MOT`). The aircraft registration prefix "SU-" denotes an aircraft
  registered in Egypt.


  ## Setting & Place

  Digital document / tabular flight tracking log screen capture. Non-physical
  interior/exterior setting.


  ## Lighting

  Uniform, flat digital UI display lighting (pure white screen background
  `#FFFFFF`, high-key, zero shadows).


  ## Cinematography

  Direct overhead/flat digital document presentation (graphic capture), full
  wide view of the data sheet.


  ## People

  No people in frame.


  ## Objects & Text

  - **Prominent Text / Large Labels**:
    - `SU-BTT` (appears in large horizontal font at bottom left and vertical orientation in mid-right column).
  - **Airport IATA Codes (partial selection from text)**:
    - `CAI` (Cairo)
    - `MED` (Medina)
    - `JED` (Jeddah)
    - `AAC` (El Arish)
    - `HRG` (Hurghada)
    - `MCT` (Muscat)
    - `SSH` (Sharm el-Sheikh)
    - `ILG` (Wilmington)
    - `PVU` (Provo)
    - `MOT` (Minot)
    - `LBG` (Paris Le Bourget)
    - `LNK` (Lincoln)
    - `OMA` (Omaha)
  - **Callsigns/Codes**: `(SUBTT)`, `EGY239`.


  ## Colors & Mood

  - **Palette**: Monochromatic base (stark white background, charcoal/black
  typography, light grey gridlines) accented with soft blue/cyan text for
  parenthetical airport abbreviations.

  - **Mood**: Analytical, informative, sterile, organized, and technical.
