source: site/internals/static/img/evidence/4ca7aec363fe0c132a1aac84b85696655a57f84b41cc318e3864c6a4d1227aa2.jpg
status: done
engine: gemini-flash-latest
provider: gemini
generated: 2026-08-20T12:47:56.338Z
kind: image
description: >-
  ## Overview

  This image is a digital screenshot of a Google Trends web interface displaying
  search analytics for the query term `“SU BND”`. The dashboard shows filter
  settings configured for the Huntsville-Decatur region over the past 90 days
  across all categories via Web Search. The primary focus is a line graph titled
  "Interest over time," displaying a single sharp spike reaching peak search
  popularity (value of 100) on September 12, 2025, while remaining flat at zero
  for the rest of the visible timeline.


  ## Hyper-Detail

  The interface is presented in standard Google Material Design styling on a
  light, pale-blue to off-white background (`#F0F4F9` style). 


  At the very top left, a small horizontal blue accent line is partially
  visible. Below this, there is an upper control panel divided into rounded
  rectangular container elements. 

  * On the left of this container sits a search term pill/box with a white
  background and rounded corners. Inside, on the left, is a solid bright blue
  circular dot, followed by the text `“SU BND”` in bold sans-serif font, with
  the sub-label `Search term` underneath in gray. To the right edge of this box
  is a vertical three-dot overflow menu icon (⋮).

  * To the right of the search term box sits a light-blue tinted button or
  container displaying a gray plus sign `+` followed by the word `Compare`.

  * Beneath the search term container is a horizontal row of four dropdown
  filter pills with rounded corners, light gray borders, and downward-pointing
  triangles:
    1. `Huntsville-Decatur ... ▼`
    2. `Past 90 days ▼`
    3. `All categories ▼`
    4. `Web Search ▼`

  Below the top configuration area, a large white card container with rounded
  corners and a very subtle drop shadow dominates the lower half of the frame.

  * The header of this section reads `Interest over time` followed by a small
  circled question mark icon (`?`).

  * On the far upper right of this white card are three dark gray action icons:
  a download arrow pointing into a tray (`↓`), an embed/code bracket icon (`<
  >`), and a share/network node icon (`<`).

  * Inside the chart area, horizontal light gray gridlines denote values on the
  y-axis, marked on the left as `100`, `75`, `50`, `25`, and `0` in a muted gray
  font.

  * The x-axis along the bottom has tick labels `Aug 1`, `Sep 1`, and `Oct 2`.

  * The blue line plot runs flat along the bottom baseline (`0`) across August
  and early September.

  * Around September 12, 2025, the blue line shoots vertically upward to touch
  the `100` line, marked with a small circular blue data point node, before
  immediately dropping straight back to zero, forming a sharp inverted "V" peak.

  * An interactive tooltip box is rendered just to the left of the peak,
  featuring a white background, thin gray border, and sharp corners. It
  contains:
    * Top line: `Sep 12, 2025` in bold black text.
    * Bottom line: `“SU BND”` in gray text on the left, aligned with a bold blue number `100` on the right.

  ## Composition

  This is a flat graphic/UI layout occupying 100% of the 2D frame.

  * **Top Third (FRAME-TOP):** Contains the search query card and filter
  controls (~30% of frame).

  * **Bottom Two-Thirds (FRAME-CENTER to FRAME-BOTTOM):** Contains the "Interest
  over time" card (~70% of frame).

  * **Chart Focal Point (FRAME-CENTER-LOW):** The blue spike and its tooltip box
  sit almost exactly at the horizontal center, drawing immediate visual
  attention due to high contrast against the surrounding white space.

  * **Symmetry & Balance:** Asymmetrical UI layout with left-aligned primary
  controls/titles and right-aligned secondary actions (Compare button, export
  icons).


  ## Location

  *(indeterminate)* – This is a digital screenshot of software/web user
  interface. The search filter references `Huntsville-Decatur`, which
  corresponds to Huntsville and Decatur in the state of Alabama, United States.


  ## Setting & Place

  Digital environment / Web browser user interface (Google Trends).


  ## Lighting

  Not applicable; flat digital graphic render with consistent, artificial UI
  styling and minimal soft dropshadows beneath UI cards.


  ## Cinematography

  Direct digital capture / screen grab. Orthographic 2D front-facing render with
  no camera lens distortion, perspective, or physical depth of field.


  ## People

  No people in frame.


  ## Objects & Text

  **Legible Text:**

  * `“SU BND”`

  * `Search term`

  * `+ Compare`

  * `Huntsville-Decatur ...`

  * `Past 90 days`

  * `All categories`

  * `Web Search`

  * `Interest over time`

  * `100`, `75`, `50`, `25`, `0`

  * `Aug 1`, `Sep 1`, `Oct 2`

  * `Sep 12, 2025`


  **Graphical Icons:**

  * Blue circular search term indicator.

  * Vertical three dots (menu).

  * Four dropdown arrows.

  * Help/info icon (circle enclosing `?`).

  * Download icon (`↓`).

  * Code embed icon (`< >`).

  * Share icon.


  ## Colors & Mood

  * **Palette:** Dominated by Google's clean interface palette: pure white
  (`#FFFFFF`), soft pale blue/gray background, slate/charcoal text, and vibrant
  Google Blue (`#1A73E8` / `#4285F4`) for the data line, indicator dot, and
  tooltip metrics.

  * **Mood:** Analytical, neutral, minimalist, informative, data-driven.
